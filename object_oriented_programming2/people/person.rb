class Person
  def initialize(name)
    @name = name
  end

  def greeting
    puts "hi my name is #{@name}"
  end
end






#we can't learn with teacher or teach with students because these are instance methods that are invisible to other sibling instances
